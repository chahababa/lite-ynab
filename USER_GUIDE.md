# Lite YNAB 使用說明書

## 這是什麼

Lite YNAB 是一套輕量化的記帳與預算管理工具，適合用來快速記錄支出、規劃每月預算、追蹤預算使用情況，以及查看支出報表。

目前專案採用：

- Next.js
- TypeScript
- Tailwind CSS
- Supabase

## 主要功能

- 登入與驗證
- 主控臺查看本月收入、已分配預算、最近交易
- 快速記帳
- 預算分配
- 預算使用情況查看
- 交易清單編輯與刪除
- 報表匯出 CSV / Excel
- 設定分類與支付方式

## 頁面導覽

- `/login`
  - 登入頁
- `/`
  - 主控臺
- `/quick-entry`
  - 快速記帳頁
- `/budget-allocation`
  - 預算分配頁
- `/budget-usage`
  - 預算使用情況頁
- `/transactions`
  - 交易清單頁
- `/reports`
  - 報表頁
- `/settings`
  - 系統設定頁

## 日常使用流程

### 1. 登入

進入系統後先登入，未登入時大多數頁面會自動導回登入頁。

### 2. 設定本月收入

到主控臺或預算分配頁輸入本月收入。

建議先完成這一步，再做後續預算分配，這樣「尚可分配」金額會更準確。

### 3. 分配本月預算

到預算分配頁，依照分類輸入本月預算。

你可以：

- 手動輸入每個分類的預算
- 複製上月預算
- 批次套用固定預算
- 調整快速記帳分類
- 新增、重新命名或刪除分類
- 管理支付方式

### 4. 記錄支出

有兩種常用方式：

- 到主控臺下方快速新增一筆支出
- 到 `/quick-entry` 用手機友善介面快速記帳

記帳時需要填寫：

- 金額
- 日期
- 分類
- 支付方式
- 備註

### 5. 查看是否超支

到 `/budget-usage` 可以看：

- 今日支出
- 本月支出
- 各分類已花多少
- 剩餘多少預算
- 哪些分類已經超支

### 6. 維護交易資料

到 `/transactions` 可查看與編輯完整交易清單。

你可以：

- 修改金額
- 修改日期
- 修改分類
- 修改支付方式
- 補上備註
- 刪除交易

### 7. 查看與匯出報表

到 `/reports` 可查看：

- 收入
- 已分配金額
- 已支出金額
- 尚可分配金額
- 分類支出
- 支付方式支出

也可以匯出：

- CSV
- Excel

## 初次開發安裝

### 1. 安裝套件

```powershell
npm install
```

### 2. 設定環境變數

建立或確認 `.env.local` 內容正確：

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
```

注意：

- 不要提交 `.env.local`
- 非必要不要修改既有 Supabase 專案環境值

### 3. 啟動開發環境

```powershell
npm run dev
```

### 4. 型別檢查與測試

```powershell
npm run typecheck
npm run test
```

## 使用上的注意事項

- 所有使用者可見文字以繁體中文、台灣用語為主
- 金額欄位為純數字輸入，通常會置中顯示
- 分類與支付方式是預算與交易的核心資料，刪除前要先確認影響
- 如果同時在 Windows 本機開著 `npm run dev`，避免再跑 `npm run build`，以免 `.next` 快取互相干擾

## 常見問題

### 看不到資料或被導回登入頁

通常代表登入狀態失效，請重新登入。

### 記帳成功但畫面沒更新

先重新整理頁面，若仍異常再確認 Supabase 寫入是否成功。

### 找不到分類或支付方式

請到設定頁或預算分配頁確認資料是否已建立，或是否被刪除。

### 匯出檔案打開後顯示亂碼

CSV 建議用支援 UTF-8 的工具開啟；若有格式需求，可優先使用 Excel 匯出版本。

## 文件參考

- `DEPLOYMENT.md`
  - 部署相關說明
- `CHANGELOG.md`
  - 變更紀錄
- `AGENTS.md`
  - 專案協作與操作規則

如果你想把這份文件改成更偏「給最終使用者」或更偏「給工程接手者」的版本，可以再往下拆成兩份。
