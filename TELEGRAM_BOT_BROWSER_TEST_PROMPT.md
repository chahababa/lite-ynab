# Telegram Bot 瀏覽器 AI 測試 Prompt

> 使用方式：
> 1. 先在瀏覽器登入 `Telegram Web`
> 2. 開啟你的 Bot 對話頁
> 3. 把下面整段 prompt 貼給可操作瀏覽器的 AI
> 4. 將中括號 placeholder 改成你的實際資訊

---

你現在是一位 QA 自動測試代理，請直接控制目前的瀏覽器分頁，對 Telegram Web 裡的 Bot 進行端到端測試。

## 測試目標

請測試這個 Bot 是否能穩定完成核心功能、正確處理錯誤輸入，並整理一份結構化測試報告。

Bot 資訊：

- Bot 名稱：`[填入 Bot 名稱]`
- Bot Username：`[填入 @bot_username]`
- 測試環境：`[例如 production / staging / local tunnel]`
- 主要功能：`[填入 3 到 8 個主要功能]`

## 測試前提

- 你必須使用目前已登入的 Telegram Web 進行測試
- 如果目前不在 Bot 對話頁，先搜尋 `@bot_username` 並進入該 Bot 對話
- 如果畫面要求登入、驗證碼、權限確認，先停下來並回報「需要人工介入」
- 除非測試流程需要，不要點擊與 Bot 無關的聊天或設定

## 操作規則

- 每一步都要先觀察畫面，再操作
- 每次送出訊息後，等待 Bot 回應完成再進入下一步
- 若回應較慢，至少等待 `[10]` 秒再判定逾時
- 如果同一步驟失敗，可重試最多 1 次
- 不要自行假設功能正確，必須以畫面上實際訊息與按鈕為準
- 如果 Bot 回傳按鈕、選單、快速回覆、連結，請一併測試

## 測試範圍

請至少覆蓋以下類型：

1. 基本啟動流程
2. 正常流程
3. 錯誤輸入流程
4. 邊界情境
5. 中斷後恢復流程
6. 多輪對話一致性

## 測試案例

請依照下面順序執行。

### A. 啟動與基礎可用性

1. 開啟 Bot 對話
2. 如果有 `/start`，先送出 `/start`
3. 記錄首則回應內容
4. 確認是否出現：
   - 歡迎訊息
   - 功能選單
   - Inline keyboard / reply buttons
   - 可理解的操作指引

### B. `/help` 或主選單

1. 如果 Bot 支援 `/help`，送出 `/help`
2. 如果沒有 `/help`，改點主選單中最接近「說明 / 幫助 / 功能列表」的按鈕
3. 驗證：
   - 功能是否有列出
   - 文字是否清楚
   - 是否有失效按鈕

### C. 核心功能測試

請依序測試以下功能，每個功能都做一次成功流程：

- 功能 1：`[填入功能與預期操作]`
- 功能 2：`[填入功能與預期操作]`
- 功能 3：`[填入功能與預期操作]`
- 功能 4：`[可選]`
- 功能 5：`[可選]`

每個功能都要記錄：

- 送出的輸入
- Bot 的回應
- 是否符合預期
- 是否有 loading 過久、回應不完整、按鈕失效、文案不清

### D. 錯誤輸入測試

請至少測以下幾種錯誤輸入：

1. 空白或無意義訊息
2. 明顯格式錯誤的輸入
3. 超長文字
4. 不在支援範圍的內容
5. 連續快速送出兩則訊息

驗證 Bot 是否：

- 有合理錯誤提示
- 不會卡死
- 不會回傳完全無關內容
- 能引導使用者回到正確流程

### E. 流程中斷與恢復

1. 先進入一個需要多步驟的流程
2. 在中途輸入無關訊息、返回主選單，或改點其他功能
3. 檢查：
   - Bot 是否有中斷保護
   - 是否會進入混亂狀態
   - 是否能重新開始

### F. 一致性檢查

請檢查：

- 同樣指令重複執行時，結果是否一致
- 按鈕文案與回覆文案是否一致
- 錯誤訊息風格是否一致
- 是否有簡體中文、英文、亂碼、占位文字混入

## 特別檢查項目

請額外注意下面問題：

- 是否有死按鈕
- 是否有點了沒反應的 inline keyboard
- 是否有重複訊息
- 是否有明顯延遲過長
- 是否有 Markdown / HTML 顯示錯誤
- 是否有圖片、連結、附件顯示異常
- 是否有流程需要人工才能繼續但沒有提示
- 是否有錯字、語句不通、亂碼

## 證據蒐集要求

如果你的環境支援，請保留以下資訊：

- 關鍵步驟截圖
- 失敗案例截圖
- 最終測試時間
- 使用的 Telegram Web 畫面狀態

如果無法截圖，至少要逐步記錄每次輸入與回應摘要。

## 報告格式

測試完成後，請用 Markdown 輸出報告，格式如下：

```md
# Telegram Bot 測試報告

## 基本資訊
- Bot：...
- 測試時間：...
- 測試環境：...

## 測試結果摘要
- 總結：通過 / 部分通過 / 失敗
- 主要問題數量：...
- 阻斷型問題：...

## 測試案例結果
| 案例 | 操作 | 預期結果 | 實際結果 | 狀態 |
|---|---|---|---|---|
| 啟動流程 | ... | ... | ... | Pass / Fail |

## 問題清單
### 1. [問題標題]
- 嚴重度：Critical / High / Medium / Low
- 重現步驟：
  1. ...
  2. ...
- 預期結果：...
- 實際結果：...
- 備註：...

## 文案與 UX 觀察
- ...

## 建議修正優先順序
1. ...
2. ...
3. ...
```

## 執行原則

- 先測完整，再下結論
- 不要只說「看起來正常」
- 要明確指出哪些功能有測、哪些沒測到
- 若遇到阻塞，請立刻說明卡在哪一步

現在開始執行測試。

---

## 建議你先替換的內容

你貼給瀏覽器 AI 前，建議先把這些補上：

- `[填入 Bot 名稱]`
- `[填入 @bot_username]`
- `[填入 3 到 8 個主要功能]`
- `[填入功能與預期操作]`
- `[10]` 秒逾時設定

## 範例補法

- Bot 名稱：`記帳小幫手`
- Bot Username：`@my_budget_bot`
- 主要功能：
  - 建立記帳
  - 查詢今日支出
  - 查詢本月支出
  - 顯示分類統計
  - 回覆操作說明

如果你要更精準，我也可以下一步直接幫你改成：

1. 通用版
2. 專門給「記帳 / 預算類 Telegram Bot」的版本
3. 專門給你這個 Bot 功能流程的客製版
