# Quick-Entry 重做 v1.0 瀏覽器 AI 驗證 Prompt

> 使用方式：
> 1. 在本機把 feature branch 跑起來（`npm run dev` → http://localhost:3000）或開啟正式環境（lite-ynab.zeabur.app）
> 2. 在瀏覽器登入 lite-ynab
> 3. 把下面整段 prompt 貼給可操作瀏覽器的 AI（例如 Claude in Chrome）
> 4. 將中括號 placeholder 改成你的實際資訊（測試環境 URL、預期可選的支付方式名稱、冷門分類名稱）

---

你現在是一位 QA 自動測試代理，請直接控制目前的瀏覽器分頁，對 lite-ynab 的 `/quick-entry` 頁面（v1.0 重做版本）進行端到端驗證。

## 測試目標

驗證 Quick Entry 介面重做（密集 1 屏 + 9 格分類 grid + 兩個 modal）有沒有破壞既有快速感、是否正確處理 10 個邊界情況、是否影響其他頁面。

## 測試環境

- 測試 URL：`[填入 http://localhost:3000 或 https://lite-ynab.zeabur.app]`
- 已登入帳號：`[填入測試用 email，例如 chahababa@hoochuu.com.tw]`
- 預期至少有 1 個常用分類（is_quick=true）與至少 1 個支付方式
- 預期至少有 1 個「冷門分類」（is_quick=false 但確實存在於 categories 表中），名稱：`[填入冷門分類，例如 房租]`
- 預期支付方式中至少有 2 個，方便切換驗證，名稱：`[填入兩個支付方式，例如 現金 / 中信卡]`

## 操作規則

- 每一步都要先觀察畫面再操作（必要時截圖記錄）
- 點按鈕後如有 toast / modal / 頁面跳轉，等待 1~2 秒再進入下一步
- 同一步驟失敗最多重試 1 次，仍失敗就記錄為 ❌
- 不要自行假設「應該會這樣」，以畫面實際呈現為準
- 進入到任何「需要登入」、「驗證碼」、「外部跳轉」的畫面，立刻停下來回報

## 測試範圍

請依下面順序執行。每個案例完成後標註 ✅ / ❌ / ⚠️ 並記錄觀察到的訊息。

---

### A. 進入與基礎可用性

A1. 進入 `/quick-entry`
- 預期：頁面載入後出現 header「記一筆」、左側 ✕ 按鈕、右側 ⚙ 設定圖示

A2. 觀察金額顯示
- 預期：頁面上方顯示紅色 `−$0`（或 `−NT$0`），字級約 20px，使用等寬字型

A3. 觀察日期 chip
- 預期：金額右邊有 pill 形 chip，顯示「今天」

A4. 觀察支付方式 chip
- 預期：pill 形 chip，顯示某個支付方式名稱（即上次使用或第一個）

A5. 觀察常用分類 grid
- 預期：3×3 grid，最後一格固定為「+ 更多」（虛線邊框 + Plus icon）；前面格子顯示常用分類名稱與左上角 6×6 圓點

A6. 觀察備註輸入框
- 預期：1 行 compact input，placeholder「備註（可留空）」

A7. 觀察 keypad
- 預期：4×3 鍵盤，數字 7-9 / 4-6 / 1-3，最後一行 `00 / 0 / ⌫`，每鍵高度感覺至少手指可按

---

### B. Happy Path：3 筆真實測試交易

B1. 第一筆 — 常用分類 + 預設支付方式
1. 點 keypad 輸入 `8` `5`（或任意金額）
2. 確認金額顯示更新成 `−$85`
3. 點任一個常用分類（例如 9 格內第一個）
4. 預期：右上角 toast 顯示「已記帳 $85 至 [分類名]」
5. 預期：金額回到 `−$0`，備註清空，日期回到「今天」，**支付方式 chip 維持不變**（保留）

B2. 第二筆 — 換支付方式後送出
1. 點支付方式 chip → 預期 PaymentMethodModal 開啟（黑色半透明 overlay + 中央卡片，標題「選擇支付方式」）
2. 列表顯示所有支付方式，當前選中的應有 highlight
3. 點選另一個支付方式（不同於 B1 用的）→ modal 關閉，chip 上的名稱更新
4. 點 keypad 輸入 `1` `2` `0`
5. 點任一常用分類送出
6. 預期：toast「已記帳 $120 至 [分類名]」+ 金額重置 + 支付方式仍是 B2 換的那個

B3. 第三筆 — 從「+ 更多」找冷門分類
1. 點 keypad 輸入 `5` `0`
2. 點 9 格中的「+ 更多」 → 預期 CategoryPickerModal 開啟（標題「選擇分類」+ 搜尋欄 + 依群組分區的列表）
3. 在搜尋框輸入 `[填入冷門分類關鍵字，例如「房」]`
4. 預期：列表只剩符合的分類
5. 點選該冷門分類 → modal 關閉 + 自動送出
6. 預期：toast「已記帳 $50 至 [冷門分類名]」+ 金額重置

B4. 跨頁驗證
1. 點左上 ✕ 或從 nav 進到 `/transactions`
2. 預期：上面 3 筆交易都出現在最新位置，金額、分類、支付方式都正確

---

### C. 邊界情況（對應 SPEC §9）

C1. 金額 = 0 阻擋送出
1. 確保金額為 `−$0`（必要時點 ⌫ 清空）
2. 點任一常用分類
3. 預期：toast「請先輸入金額」+ 沒有跳轉、沒有寫入

C2. 9 格 grid 動態長度
- 已在 A5 觀察到。額外確認：如果你的常用分類少於 8 個，「+ 更多」應該緊接在實際分類後面，不會留空白格子撐到 9 格

C3. 重複快速 click 鎖
1. 輸入金額 `1`
2. 連續快速點擊同一個常用分類兩次（盡量同時）
3. 預期：只送出 1 筆（去 `/transactions` 確認），第二次點擊應被 isSubmitting 鎖住（按鈕變灰）

C4. 備註輸入
1. 輸入金額 `3` `0`
2. 在備註輸入「測試備註 abc」
3. 點分類送出
4. 去 `/transactions` 確認該筆交易的備註欄位有正確寫入

C5. 日期切換
1. 點日期 chip → 出現原生 prompt 視窗，輸入 `2026-04-01`，按 OK
2. 預期：chip 上的「今天」變成「2026-04-01」
3. 輸入金額 `1`，點分類送出
4. 預期：toast 成功，chip 重置回「今天」（這部分依 spec 是送出後重置）
5. 額外驗證：如果再點日期 chip 出現 prompt，留空按 OK，chip 應變回「今天」

C6. PaymentMethodModal 各種關閉方式
1. 點支付方式 chip 開啟
2. **方式 A**：按鍵盤 `Escape` → 預期關閉
3. 再次開啟，**方式 B**：點 modal 卡片以外的黑色背景 → 預期關閉
4. 再次開啟，**方式 C**：點卡片右上 ✕ → 預期關閉
5. 再次開啟，**方式 D**（手機 Chrome 才測得到）：按 Android 系統返回鍵 → 預期 modal 關閉而不是離開頁面
6. 開啟時試著捲動 modal 後面的頁面 → 預期捲不動（body scroll lock）

C7. CategoryPickerModal 同上
- 重複 C6 的 4 種關閉方式（A/B/C/D + scroll lock）對「+ 更多」開的 CategoryPickerModal 重新測一次

C8. 搜尋無結果
1. 開啟 CategoryPickerModal
2. 在搜尋輸入「zzzzzzzzz」
3. 預期：顯示提示文字「找不到符合搜尋條件的分類」

C9. localStorage 持久化
1. 在 PaymentMethodModal 換到某個支付方式 X
2. 重新整理頁面（F5 或 Cmd+R）
3. 預期：頁面重新載入後，支付方式 chip 仍顯示 X（從 localStorage 讀回來）

C10. （選做，如果你的測試帳號可以）沒有支付方式的引導
- 如果有測試帳號的支付方式為 0，預期頁面上方顯示黃色 banner「尚未建立支付方式，請先到設定建立後再記帳」+ 嘗試送出時 toast「請先到設定建立支付方式」+ 不會寫入

---

### D. 跨頁面回歸（避免改 quick-entry 影響其他頁面）

對下面 5 個頁面，分別進入並確認頁面正常 render（沒有 console error、沒有空白頁、主要 UI 元素還在）：

| 頁面 | 確認重點 |
|---|---|
| `/` | 主控臺顯示正常 |
| `/transactions` | 交易清單顯示，B 段 3 筆都看得到 |
| `/budget-allocation` | 預算分配畫面顯示正常 |
| `/reports` | 報表頁顯示正常（注意月份範圍正確） |
| `/settings` | 設定頁顯示正常 |

---

### E. （Bonus）Console 與 Network 觀察

- 整個測試過程中打開 DevTools，記錄是否出現任何 `console.error` 或 `console.warn`
- 觀察 Network → 送出時應只有 1 個 POST `/rest/v1/transactions` 的 supabase 請求

---

## 測試報告格式

最後請輸出一份 Markdown 報告，包含：

```markdown
# Quick Entry v1.0 驗證報告

## 環境
- URL：
- 帳號：
- 瀏覽器：
- 測試時間：

## 結果摘要
- 共執行 X 個案例：✅ Y 個 / ⚠️ Z 個 / ❌ W 個

## 詳細結果

### A. 進入與基礎可用性
- A1 ✅/❌：[觀察]
- A2 ...
（依此類推）

### B. Happy Path
...

### C. 邊界情況
...

### D. 跨頁面回歸
...

### E. Console / Network
...

## 失敗 / 異常項目（若有）
- [描述、截圖路徑、可能原因]

## 建議
- [是否可發布、有沒有需要先修的 blocker]
```

完成後把報告貼回給我，我會根據結果決定是否 merge 進 main 並走 Zeabur 部署。
